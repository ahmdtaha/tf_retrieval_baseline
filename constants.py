# Where is the datasets' folders are (stanford online, CUB)?
dataset_dir = '/mnt/data/datasets/'

# where is the pre-trained model saved (inception, resnet, densenet) ?
trained_models_dir = '/mnt/data/pretrained/'

# Where to save a new experiment details (log, tensorboard, trained retrieval models)
experiment_root_dir = '/mnt/data/checkpoints/retrieval_models/'